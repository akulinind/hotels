<tr>
	<td><%= user.id %></td>
	<td><%= user.name %></td>
	<td><%= user.email %></td>
	<td><%= user.hotels.count %></td>
	<td><%= user.rates.count %></td>
	<td><%= link_to "delete", user, method: :delete, data: { confirm: "You sure?" }, title: user.name %></td>
	<td><%= link_to "edit", edit_user_path(user.id) %></td>
</tr>